# Runtime dependencies for the Vercel serverless deployment (API only).
#
# Intentionally excludes:
#   - uvicorn[standard] / uvloop / httptools: Vercel provides the ASGI server.
#     Shipping uvloop is what triggered the cold-start
#     "OSError: [Errno 16] Device or resource busy"; standard asyncio is used.
#   - aiosqlite: SQLite is only used for local/dev/test, never on Vercel.
fastapi>=0.115
pydantic>=2.10
pydantic-settings>=2.7
pydantic-ai-slim[anthropic]>=0.0.30
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30
greenlet>=3.1
pyjwt>=2.10
httpx>=0.28
